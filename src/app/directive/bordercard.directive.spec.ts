import { BordercardDirective } from './bordercard.directive';

describe('BordercardDirective', () => {
  it('should create an instance', () => {
    const directive = new BordercardDirective();
    expect(directive).toBeTruthy();
  });
});
