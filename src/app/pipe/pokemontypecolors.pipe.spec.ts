import { PokemontypecolorsPipe } from './pokemontypecolors.pipe';

describe('PokemontypecolorsPipe', () => {
  it('create an instance', () => {
    const pipe = new PokemontypecolorsPipe();
    expect(pipe).toBeTruthy();
  });
});
